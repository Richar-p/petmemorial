doctype html
html lang=I18n.locale
  head
    = render partial: 'layouts/seo'
    = csrf_meta_tags
    = csp_meta_tag
    = favicon_link_tag '/icon-no-bg.png'
    = javascript_pack_tag 'style'
    = stylesheet_link_tag "//ajax.googleapis.com/ajax/libs/jqueryui/1.13.1/themes/#{@theme.eql?('white') ? 'base' : 'vader'}/jquery-ui.min.css"
    script src="https://kit.fontawesome.com/a5815e2125.js" crossorigin="anonymous"

  body style=('background: #FFF !important' if @theme.eql?('white'))
    h1 class="no-display" PetMemorial 🐶🐱
    = yield
    = render partial: 'flash' unless params['tv'].present?

